# send-push
Deploy with Supabase CLI and set secrets: VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY and VAPID_SUBJECT.
The public key must also be placed in Cloudflare Pages as VITE_VAPID_PUBLIC_KEY, then rebuild the site.
